A PGM image (Portable Gray Map) is a simple file format used to store 2D grayscale images, where each pixel represents a shade of gray.
To convert a PGM image ising ImageMagik:
 ```
convert input.jpg -colorspace Gray output.pgm